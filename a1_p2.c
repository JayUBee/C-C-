#include <stdio.h>
int main() {
int result; /* The result of our calculation */
result = (2 + 7) * (9 / 3);
printf("The result is %d\n", result);
/*for the program to return the correct value we had to tell the program
what value needs to be assigned to &d, which is result*/
return 0;
}
